

- **核心特性**：

  - **无头模式**：支持后台静默运行，不占用桌面，不干扰正常工作。## 🔥 核心功能：Discord 自动登录工具 (Discord Auto Login)

  - **多线程并发**：支持同时启动多个浏览器窗口（建议 3-5 线程），批量处理效率极高。

  - **WebSocket 注入**：直接通过浏览器调试接口注入 Token，无需模拟键盘输入，速度快且稳定。

  - **智能风控识别**：自动识别账号是否被标记、是否需要手机验证或已被禁用，并分类统计。

  - **独立运行**：提供 `.bat` 脚本，无需安装环境即可在任何 Windows 电脑上运行。这是本工具箱的核心功能，专为 **MoreLogin 指纹浏览器** 用户设计，实现 Discord 账号的自动化免密登录。---这是一个集成了多种实用脚本的综合性工具平台，专为 Web3 玩家、数据分析师和自动化操作者设计。项目基于 Next.js 构建，提供现代化的 Web 界面，支持本地部署和独立打包运行。这是一个基于 Next.js 构建的综合性工具箱，旨在为 Web3 开发者、数据分析师和普通用户提供便捷的在线工具。



- **使用方法**：

  1. 打开 MoreLogin 浏览器，确保“本地 API”已开启（端口 40000）。

  2. 在工具设置中填入 MoreLogin 的 `App ID` 和 `Secret Key`。### ✨ 功能特性

  3. 输入 `ProfileID----Token` 格式的账号列表。

  4. 点击开始，工具将自动调度浏览器完成登录。- **无头模式支持**：支持后台静默运行，不干扰前台操作。



---- **多线程并发**：支持同时启动多个浏览器窗口进行登录，极大提高效率。## 🌟 核心功能亮点



## 🛠️ 全能工具箱清单- **自动 Token 注入**：通过 WebSocket 连接浏览器，自动注入 Token 完成登录，无需手动输入密码。



以下工具按功能分类排列，每个工具都经过精心优化，满足特定场景需求。- **智能状态检测**：自动识别账号状态（正常、被标记、需要验证等）。



### 📧 邮箱与推荐工具- **独立运行包**：提供 `start.bat` 一键启动的独立运行包，无需安装 Node.js 环境即可在同事电脑上运行。

1. **Firstmail 邮箱收信 (Firstmail Receiver)**

   - **功能**：批量管理 Firstmail.ltd 邮箱。### 🔥 Discord 自动登录工具 (Discord Auto Login)---## 🛠️ 功能列表

   - **详情**：通过 API 一键收取所有邮箱的最新邮件，支持提取验证码，数据仅本地存储，安全高效。

### 📖 使用步骤

2. **MoreLogin 指纹浏览器 (Recommendation)**

   - **功能**：跨海电商与多账号管理推荐。1. **准备工作**：这是本工具箱的核心功能，专为 MoreLogin 指纹浏览器用户设计，实现 Discord 账号的自动化免密登录。

   - **详情**：提供隔离的指纹环境，防止账号关联，是本工具箱核心功能的最佳搭档。

   - 确保本地已安装并登录 [MoreLogin](https://www.morelogin.com/) 指纹浏览器。

### 📊 数据处理工具 (Data Processing)

3. **列表去重清洗 (List Cleaner)**   - 确保 MoreLogin 的本地 API 端口已开启（默认 40000）。

   - **功能**：文本列表的清洗与标准化。

   - **详情**：支持一键去除重复项、删除空行、打乱列表顺序、去除首尾空格，是处理账号库的第一步。2. **配置 API**：



4. **格式转换工具 (Format Converter)**   - 在工具界面的设置中，填入 MoreLogin 的 `App ID` 和 `Secret Key`。**功能特性：**

   - **功能**：账号/数据格式的灵活转换。

   - **详情**：支持自定义输入和输出模板（如将 `User:Pass:Email` 转换为 `Email----Pass`），支持自定义分隔符。   - 获取路径：MoreLogin 客户端 -> 设置 -> 本地 API。



5. **文本提取工具 (Text Extractor)**3. **导入数据**：- **无头模式支持**：支持后台静默运行，不干扰前台操作。## 🌟 核心功能亮点本项目包含以下几类工具：

   - **功能**：从杂乱文本中提取关键信息。

   - **详情**：内置正则表达式，可一键提取文本中的 **邮箱**、**URL 链接**、**IP 地址**、**私钥** 等信息。   - 支持粘贴 `ProfileID` 列表（仅启动环境）。



6. **Cookie 格式转换 (Cookie Converter)**   - 支持 `ProfileID----Token` 格式（启动并自动登录）。- **多线程并发**：支持同时启动多个浏览器窗口进行登录，极大提高效率。

   - **功能**：Cookie 格式互转。

   - **详情**：支持 JSON 格式（EditThisCookie）与 Netscape 格式（浏览器标准）之间的相互转换。4. **开始运行**：



7. **列表交集差集 (List Diff)**   - 设置并发线程数（建议 3-5）。- **自动 Token 注入**：通过 WebSocket 连接浏览器，自动注入 Token 完成登录，无需手动输入密码。

   - **功能**：两个列表的对比分析。

   - **详情**：快速找出两个列表的 **交集**（共有部分）、**差集**（A有B无、B有A无），支持自动去重。   - 点击“开始执行”，工具将自动调用本地浏览器接口打开窗口并完成登录。



8. **批量编码加密 (Batch Encoder)**- **智能状态检测**：自动识别账号状态（正常、被标记、需要验证等）。

   - **功能**：常用编码与哈希计算。

   - **详情**：支持 Base64、URL Encode/Decode、Hex 编码，以及 MD5、SHA1、SHA256 等哈希值计算。---



9. **邮箱处理工具 (Email Processor)**- **独立运行包**：提供 `start.bat` 一键启动的独立运行包，无需安装 Node.js 环境即可在同事电脑上运行。### 🔥 Discord 自动登录工具 (Discord Auto Login)### 📊 数据处理 (Data Processing)

   - **功能**：邮箱列表的高级处理。

   - **详情**：支持提取邮箱域名、去除重复邮箱、生成邮箱别名（如 Gmail 的 + 别名），支持导出 Excel。## 🛠️ 工具箱详细清单



### 🔍 账号查询工具 (Account Query)

10. **Discord Token 检测 (Discord Checker)**

    - **功能**：批量检测 Discord Token 有效性。### 📧 邮箱工具 (Email Tools)

    - **详情**：快速验证 Token 是否存活，支持配置代理 IP 进行轮询检测，防止因 IP 封禁导致的误判。

- **Firstmail 邮箱收信**: 批量管理 Firstmail.ltd 邮箱，一键收取所有邮件。数据本地存储，安全高效。**使用步骤：**这是本工具箱的核心功能，专为 MoreLogin 指纹浏览器用户设计，实现 Discord 账号的自动化免密登录。- **文本提取 (Text Extractor)**: 从复杂的文本中提取特定格式的数据（如邮箱、URL、IP地址等）。

11. **推特账号查询 (Twitter Checker)**

    - **功能**：Twitter 账号状态批量检测。

    - **详情**：检测账号是否正常、被冻结或受限，支持获取账号的粉丝数、注册时间等详细信息。

### 🌍 跨海工具推荐 (Recommendation)1. **准备工作**：确保本地已安装并登录 [MoreLogin](https://www.morelogin.com/) 指纹浏览器。

### 🛠️ 便捷实用工具 (Convenient)

12. **IP 纯净度检测 (Proxy Checker)**- **MoreLogin 指纹浏览器**: 专业的指纹浏览器，多账号防关联管理神器。提供真实指纹环境，安全高效。

    - **功能**：代理 IP 质量分析。

    - **详情**：批量测试 SOCKS5/HTTP 代理的连通性，查询 IP 归属地，并识别 IP 类型（机房/住宅/蜂窝网络）。2. **配置 API**：在工具设置中填入 MoreLogin 的 `App ID` 和 `Secret Key`（在 MoreLogin 设置 -> 本地 API 中获取）。- **批量转账 (Batch Transfer)**: (开发中) 批量生成转账数据或执行转账操作。



13. **2FA 动态码生成 (2FA Generator)**### 📊 数据处理 (Data Processing)

    - **功能**：Google Authenticator 桌面版。

    - **详情**：批量导入 2FA Secret Key，实时计算 6 位动态验证码，点击即可复制，方便快捷。- **列表去重清洗**: 高效处理文本列表。支持一键去重、去除空行、打乱顺序、去除首尾空格。3. **导入数据**：支持粘贴 `ProfileID` 列表，或 `ProfileID----Token` 格式的账号数据。



14. **强密码生成器 (Password Generator)**- **格式转换工具**: 灵活转换账号/代理格式。支持自定义分隔符与输出模板，轻松将 `user:pass` 转换为 JSON 或其他格式。

    - **功能**：批量生成安全密码。

    - **详情**：自定义密码长度，可选包含大写字母、小写字母、数字、特殊符号，支持批量生成并导出。- **文本提取工具**: 智能文本清洗与提取。支持自定义分隔符、列选择、正则提取，轻松处理日志、Cookie 及各类结构化文本。4. **开始运行**：点击“开始执行”，工具将自动调用本地浏览器接口打开窗口并完成登录。**功能特性：**



15. **User-Agent 生成 (UA Generator)**- **Cookie 格式转换**: Netscape 与 JSON 格式 Cookie 互转，支持批量处理与格式化。

    - **功能**：模拟不同设备指纹。

    - **详情**：批量生成随机的 User-Agent 字符串，支持筛选操作系统（Windows/Mac/iOS/Android）和浏览器类型。- **列表交集差集**: 比较两个文本列表，快速计算交集、并集、差集，支持去重。



### 🌐 Web3 专用工具 (Web3)- **批量编码加密**: 支持 Base64、URL、Hex 编码转换，以及 MD5、SHA 等哈希计算。

16. **Web3 批量钱包生成 (Wallet Generator)**

    - **功能**：批量创建加密货币钱包。- **邮箱处理工具**: 批量提取、过滤、去重和生成邮箱别名，支持 Excel 导出。---- **无头模式支持**：支持后台静默运行，不干扰前台操作。### 🛠️ 便捷工具 (Convenient)

    - **详情**：本地离线生成以太坊（EVM）兼容链的钱包地址、私钥和助记词，支持一键导出 Excel，绝对安全。



17. **Web3 批量转账/归集 (Batch Transfer)**

    - **功能**：代币分发与资金归集。### 🔍 账号查询 (Account Query)

    - **详情**：支持 ETH、BSC、Polygon 等多条链。可连接钱包进行批量转账，或导入私钥进行资金归集。

- **Discord Token 检测**: 批量检测 Token 有效性，支持代理 IP 轮询，防止封号。

---

- **Discord 自动登录**: (见上方核心功能介绍)## 🛠️ 工具箱详细清单- **多线程并发**：支持同时启动多个浏览器窗口进行登录，极大提高效率。- **代理检测 (Proxy Checker)**: 检测 SOCKS5/HTTP 代理的连通性、匿名度及 IP 风险信息。

## 🚀 部署与运行指南

- **推特账号查询**: 查询推特账号信息，支持批量检测账号状态及获取详细资料。

### 方式一：独立运行包 (推荐给测试同事)

**最简单的方式，无需配置环境。**

1. 获取 `discord-login-tool.zip` 压缩包并解压。

2. 进入解压后的文件夹，双击运行 **`start.bat`**。### 🛠️ 便捷工具 (Convenient)

3. 等待浏览器自动弹出工具界面即可使用。

- **IP 纯净度检测**: 批量检测代理 IP 质量。支持延迟测试、归属地查询及 IP 类型识别(机房/住宅)。本项目包含以下四大类工具：- **自动 Token 注入**：通过 WebSocket 连接浏览器，自动注入 Token 完成登录，无需手动输入密码。- **2FA 生成器 (2FA Generator)**: 在线生成 Google Authenticator 两步验证码。

### 方式二：开发者源码运行

1. 安装 Node.js 18+ 环境。- **2FA 动态码生成**: 批量导入 2FA 密钥，实时生成动态验证码。支持备注区分，点击一键复制。

2. 根目录运行 `npm install` 安装依赖。

3. 运行 `npm run dev` 启动开发服务，访问 `http://localhost:3000`。- **强密码生成器**: 生成高强度随机密码。支持自定义长度、字符集（大小写/数字/符号）及批量生成导出。



### 方式三：服务器生产部署- **User-Agent 生成**: 批量生成随机 User-Agent。支持筛选操作系统与浏览器类型，模拟真实访问环境。

1. 运行 `npm run build` 构建项目。

2. 运行 `npm start` 启动生产服务。### 1. 自动化与账号工具- **智能状态检测**：自动识别账号状态（正常、被标记、需要验证等）。

3. 建议使用 PM2 进行进程守护：`pm2 start npm --name "toolbox" -- start`。

### 🌐 Web3 工具 (Web3)

---

- **Web3 批量钱包生成**: 批量生成多链钱包地址、私钥及助记词。支持导出 Excel，完全本地离线运行，安全无忧。- **Discord 登录 (Discord Login)**: 上述核心功能，批量自动登录 Discord。

## ⚠️ 重要说明

- **数据隐私**：本项目所有数据处理逻辑均在 **本地浏览器** 或 **本地服务器** 完成，不会上传任何数据到第三方服务器。- **Web3 批量转账/归集**: 支持多链代币批量发送与归集。连接钱包授权发送，或导入私钥批量归集。本地运行，安全可靠。

- **API 依赖**：部分功能（如 Discord 登录）依赖本地 MoreLogin 环境，请确保相关软件已正确运行。

- **Discord 检测 (Discord Checker)**: 批量检测 Discord Token 的有效性及账号信息。- **独立运行包**：提供 `start.bat` 一键启动的独立运行包，无需安装 Node.js 环境即可在同事电脑上运行。### 🔍 账号查询 (Account Query)

---

- **Twitter 检测 (Twitter Checker)**: 批量检测 Twitter 账号状态（正常/封禁/受限）。

## 🚀 部署与运行指南

- **MoreLogin 管理 (MoreLogin Tools)**: 批量启动/停止/管理指纹浏览器环境。- **推特检测 (Twitter Checker)**: (开发中) 批量检测 Twitter 账号状态。

### 方式一：独立运行包 (推荐给测试同事)

适合直接发给同事使用，无需配置环境。- **Firstmail 接码 (Firstmail Receiver)**: 对接 Firstmail API，批量读取邮箱验证码。



1. 在项目根目录找到 `public/downloads/discord-login-tool.zip`（如果未生成，请运行打包脚本）。**使用步骤：**

2. 解压压缩包。

3. 双击运行文件夹内的 **`start.bat`**。### 2. 数据处理与转换

4. 浏览器将自动打开工具界面，即可开始使用。

- **文本提取 (Text Extractor)**: 从杂乱文本中智能提取 邮箱、URL、IP、私钥 等特定格式数据。1. **准备工作**：确保本地已安装并登录 [MoreLogin](https://www.morelogin.com/) 指纹浏览器。### 🌐 Web3 工具

### 方式二：开发者模式 (源码运行)

适合开发人员或需要修改代码的用户。- **格式转换 (Format Converter)**: 账号格式互转（如 `User:Pass:Email` 转 `Email:Pass`）。



1. **环境要求**：Node.js 18+- **Cookie 转换 (Cookie Converter)**: JSON 格式与 Netscape 格式 Cookie 互转。2. **配置 API**：在工具设置中填入 MoreLogin 的 `App ID` 和 `Secret Key`（在 MoreLogin 设置 -> 本地 API 中获取）。- **钱包生成 (Wallet Generator)**: 批量生成 ETH/EVM 兼容链的钱包地址和私钥。

2. **安装依赖**：

   ```bash- **列表清洗 (List Cleaner)**: 文本去重、去除空行、打乱顺序、提取特定列。

   npm install

   ```- **列表对比 (List Diff)**: 对比两个文本列表，找出交集、差集（A有B无、B有A无）。3. **导入数据**：支持粘贴 `ProfileID` 列表，或 `ProfileID----Token` 格式的账号数据。

3. **启动服务**：

   ```bash- **批量编码 (Batch Encoder)**: 批量进行 Base64、URL、Hex 等编码/解码。

   npm run dev

   ```4. **开始运行**：点击“开始执行”，工具将自动调用本地浏览器接口打开窗口并完成登录。## 🚀 快速开始

   访问 [http://localhost:3000](http://localhost:3000)

### 3. 安全与生成器

### 方式三：生产环境部署

适合部署在服务器上长期运行。- **2FA 生成器 (2FA Generator)**: 批量计算 Google Authenticator 验证码（基于 Secret Key）。



1. **构建项目**：- **密码生成 (Password Generator)**: 批量生成高强度随机密码。

   ```bash

   npm run build- **UA 生成器 (UA Generator)**: 批量生成不同设备/浏览器的 User-Agent 字符串。---### 开发环境

   ```

2. **启动服务**：- **代理检测 (Proxy Checker)**: 批量测试 SOCKS5/HTTP 代理的连通性、匿名度及 IP 归属地。

   ```bash

   npm start

   ```

### 4. Web3 专用工具

---

- **钱包生成 (Wallet Generator)**: 批量生成以太坊 (EVM) 钱包地址和私钥，支持导出 Excel。## 🛠️ 工具箱详细清单1. **安装依赖**:

## ⚠️ 注意事项

- **批量转账 (Batch Transfer)**: 批量生成转账数据或执行转账操作，支持多链（ETH, BSC, Polygon等）。

- **本地 API 端口**：Discord 登录工具依赖 MoreLogin 的本地 API，默认端口为 `40000`。请确保 MoreLogin 软件已开启且端口未被占用。

- **数据安全**：所有数据处理均在本地浏览器端或本地服务器完成，不会上传到任何第三方云端，请放心使用。   ```bash

- **API 限制**：使用 MoreLogin 接口时，请注意并发数量，建议根据电脑配置设置线程数（推荐 3-5 线程）。

---

---

本项目包含以下四大类工具：   npm install

## 🤝 贡献与反馈

## 🚀 部署与运行指南

如果您在使用过程中发现 Bug 或有新功能建议，请提交 Issue 或 Pull Request。

   # 或者

### 方式一：开发者模式 (源码运行)

适合开发人员或需要修改代码的用户。### 1. 自动化与账号工具   yarn install



1. **环境要求**：Node.js 18+- **Discord 登录 (Discord Login)**: 上述核心功能，批量自动登录 Discord。   ```

2. **安装依赖**：

   ```bash- **Discord 检测 (Discord Checker)**: 批量检测 Discord Token 的有效性及账号信息。

   npm install

   ```- **Twitter 检测 (Twitter Checker)**: 批量检测 Twitter 账号状态（正常/封禁/受限）。2. **启动开发服务器**:

3. **启动服务**：

   ```bash- **MoreLogin 管理 (MoreLogin Tools)**: 批量启动/停止/管理指纹浏览器环境。   ```bash

   npm run dev

   ```- **Firstmail 接码 (Firstmail Receiver)**: 对接 Firstmail API，批量读取邮箱验证码。   npm run dev

   访问 [http://localhost:3000](http://localhost:3000)

   ```

### 方式二：生产环境部署

适合部署在服务器上长期运行。### 2. 数据处理与转换



1. **构建项目**：- **文本提取 (Text Extractor)**: 从杂乱文本中智能提取 邮箱、URL、IP、私钥 等特定格式数据。3. **访问**:

   ```bash

   npm run build- **格式转换 (Format Converter)**: 账号格式互转（如 `User:Pass:Email` 转 `Email:Pass`）。   打开浏览器访问 [http://localhost:3000](http://localhost:3000)

   ```

2. **启动服务**：- **Cookie 转换 (Cookie Converter)**: JSON 格式与 Netscape 格式 Cookie 互转。

   ```bash

   npm start- **列表清洗 (List Cleaner)**: 文本去重、去除空行、打乱顺序、提取特定列。### 生产环境构建

   ```

- **列表对比 (List Diff)**: 对比两个文本列表，找出交集、差集（A有B无、B有A无）。

### 方式三：独立运行包 (推荐给测试同事)

适合直接发给同事使用，无需配置环境。- **批量编码 (Batch Encoder)**: 批量进行 Base64、URL、Hex 等编码/解码。```bash



1. 在项目根目录找到 `public/downloads/discord-login-tool.zip`（如果未生成，请运行打包脚本）。npm run build

2. 解压压缩包。

3. 双击运行文件夹内的 **`start.bat`**。### 3. 安全与生成器npm start

4. 浏览器将自动打开工具界面，即可开始使用。

- **2FA 生成器 (2FA Generator)**: 批量计算 Google Authenticator 验证码（基于 Secret Key）。```

---

- **密码生成 (Password Generator)**: 批量生成高强度随机密码。

## ⚠️ 注意事项

- **UA 生成器 (UA Generator)**: 批量生成不同设备/浏览器的 User-Agent 字符串。## 📦 部署指南

- **本地 API 端口**：Discord 登录工具依赖 MoreLogin 的本地 API，默认端口为 `40000`。请确保 MoreLogin 软件已开启且端口未被占用。

- **数据安全**：所有数据处理均在本地浏览器端或本地服务器完成，不会上传到任何第三方云端，请放心使用。- **代理检测 (Proxy Checker)**: 批量测试 SOCKS5/HTTP 代理的连通性、匿名度及 IP 归属地。

- **API 限制**：使用 MoreLogin 接口时，请注意并发数量，建议根据电脑配置设置线程数（推荐 3-5 线程）。

### 部署到 Vercel

---

### 4. Web3 专用工具本项目完美支持 Vercel 部署。只需将代码推送到 GitHub，然后在 Vercel 中导入即可。

## 🤝 贡献与反馈

- **钱包生成 (Wallet Generator)**: 批量生成以太坊 (EVM) 钱包地址和私钥，支持导出 Excel。

如果您在使用过程中发现 Bug 或有新功能建议，请提交 Issue 或 Pull Request。

- **批量转账 (Batch Transfer)**: (开发中) 简单的批量代币分发工具。### 部署到宝塔面板 (Baota Panel)



---1. **环境准备**:

   - 安装 Node.js v18+

## 🚀 部署与运行指南   - 安装 PM2 管理器



### 方式一：开发者模式 (源码运行)2. **部署步骤**:

适合开发人员或需要修改代码的用户。   - 上传代码到服务器（排除 `node_modules`）。

   - 在项目根目录运行 `npm install`。

1. **环境要求**：Node.js 18+   - 运行 `npm run build` 进行构建。

2. **安装依赖**：   - 使用 PM2 启动: `pm2 start npm --name "toolbox" -- start`。

   ```bash

   npm install## 📝 技术栈

   ```

3. **启动服务**：- **框架**: Next.js 14 (App Router)

   ```bash- **样式**: Tailwind CSS

   npm run dev- **图标**: Lucide React

   ```- **UI 组件**: 自定义组件

   访问 [http://localhost:3000](http://localhost:3000)- **代理检测**: `socks-proxy-agent`, `axios`



### 方式二：生产环境部署## 📄 许可证

适合部署在服务器上长期运行。

MIT License

1. **构建项目**：

   ```bash   - 运行 `npm install` 安装依赖。

   npm run build3. **构建项目**：

   ```   - 运行 `npm run build` 进行打包。

2. **启动服务**：4. **启动服务**：

   ```bash   - 方式一（推荐）：使用 PM2 启动

   npm start     ```bash

   ```     pm2 start npm --name "toolbox" -- start

     ```

### 方式三：独立运行包 (推荐给测试同事)   - 方式二：使用宝塔的“Node项目”功能

适合直接发给同事使用，无需配置环境。     - 添加 Node 项目

     - 启动选项选择 `start` 脚本

1. 在项目根目录找到 `public/downloads/discord-login-tool.zip`（如果未生成，请运行打包脚本）。     - 端口设置为 `3000`（或其他自定义端口）

2. 解压压缩包。5. **反向代理**：

3. 双击运行文件夹内的 **`start.bat`**。   - 在宝塔网站设置中，添加反向代理。

4. 浏览器将自动打开工具界面，即可开始使用。   - 目标 URL：`http://127.0.0.1:3000`

   - 发送域名：`$host`

---

### 3. 注意事项

## ⚠️ 注意事项- 如果修改了代码，需要重新运行 `npm run build` 并重启 PM2 进程。

- 确保服务器防火墙放行了相应端口（如果直接访问端口的话）。

- **本地 API 端口**：Discord 登录工具依赖 MoreLogin 的本地 API，默认端口为 `40000`。请确保 MoreLogin 软件已开启且端口未被占用。

- **数据安全**：所有数据处理均在本地浏览器端或本地服务器完成，不会上传到任何第三方云端，请放心使用。
- **API 限制**：使用 MoreLogin 接口时，请注意并发数量，建议根据电脑配置设置线程数（推荐 3-5 线程）。

